[package]
name = "localgpt-core"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Core library for LocalGPT — agent, memory, config, security"
readme = "README.md"

[features]
default = ["embeddings-local", "claude-cli", "gemini-cli", "codex-cli", "sqlite-vec", "image-resize"]
# Local embeddings via fastembed (ONNX). Works on desktop and mobile.
embeddings-local = ["fastembed"]
# Claude CLI provider (requires subprocess execution — not available on mobile)
claude-cli = []
# Gemini CLI provider (requires subprocess execution — not available on mobile)
gemini-cli = []
# Codex CLI provider (requires subprocess execution — not available on mobile)
codex-cli = []
# GGUF embedding model support via llama.cpp (requires C++ compiler)
embeddings-gguf = ["llama-cpp-2"]
# OpenAI API embeddings (no native deps, requires API key)
embeddings-openai = []
# Disable all embeddings — FTS5 keyword search only
embeddings-none = []
# Sqlite vector search extension (works on mobile)
sqlite-vec = ["dep:sqlite-vec"]
# Image optimization for vision models (resizes before sending to LLM)
image-resize = ["dep:image"]
# MCP streamable HTTP transport (adds axum + tower-http — not for mobile)
mcp-http = ["dep:axum", "dep:tower-http"]
# Legacy alias
gguf = ["embeddings-gguf"]

[dependencies]
tokio = { workspace = true }
reqwest = { workspace = true }
rusqlite = { workspace = true }
sqlite-vec = { workspace = true, optional = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true }
chrono = { workspace = true }
uuid = { workspace = true }
async-trait = { workspace = true }
futures = { workspace = true }
anyhow = { workspace = true }
thiserror = { workspace = true }
shellexpand = { workspace = true }
rand = { workspace = true }

# Local embeddings (default - no API key needed)
fastembed = { version = "5.12", optional = true }
# GGUF embeddings via llama.cpp (optional)
llama-cpp-2 = { version = "0.1", optional = true }

# Image optimization
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp"], optional = true }

# MCP HTTP transport (optional — desktop only)
axum = { version = "0.8", features = ["macros"], optional = true }
tower-http = { version = "0.6", features = ["cors"], optional = true }

# Cron expression parsing
croner = "2"

# Config
json5 = "1.3"

# File watching
notify = "8.2"

# Path resolution
directories = "6.0"
etcetera = "0.11"
libc = "0.2"

# Utilities
which = "8"
tokio-stream = "0.1"
async-stream = "0.3"
glob = "0.3"
base64 = "0.22"
regex = "1"
once_cell = "1"
fs2 = "0.4"
readability = "0.3"
tempfile = "3.27"

# Security (HMAC signing, hashing, encryption)
sha2 = "0.10"
hmac = "0.12"
chacha20poly1305 = "0.10"
zeroize = { version = "1", features = ["derive"] }

# JWT signing (for Vertex AI service account auth)
jsonwebtoken = "9"

[target.'cfg(target_os = "linux")'.build-dependencies]
cc = "1"

[dev-dependencies]
tempfile = "3.27"
mockall = "0.14"
filetime = "0.2"
